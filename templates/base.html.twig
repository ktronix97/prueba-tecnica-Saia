<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <title>{% block title %}Mi App{% endblock %}</title>

    {# Bootstrap 5 y FontAwesome desde CDN #}
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css"
      rel="stylesheet"
    />

    {% block stylesheets %}{% endblock %}
  </head>
  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
      <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">🗂️ Gestión</a>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link d-none" href="/dashboard" id="nav-dashboard">Reportes</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login" id="nav-login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link d-none" href="#" id="nav-logout">Cerrar sesión</a>
          </li>
        </ul>

      </div>
    </nav>

    <main class="container my-5">
    {% block body %}
      <h1 class="mb-4">Dashboard</h1>
      <p>Bienvenida, <span id="user-email" class="fw-bold text-primary"></span></p>

      <section id="app-tasks" class="mb-5">
        <div class="alert alert-info">Cargando tareas...</div>
      </section>

      <section id="app-reports" class="mb-5">
        <div class="d-flex gap-2">
          <button class="btn btn-outline-primary" id="btn-download-pdf">
            <i class="fas fa-file-pdf"></i> Descargar PDF
          </button>
          <button class="btn btn-outline-success" id="btn-download-csv">
            <i class="fas fa-file-csv"></i> Descargar CSV
          </button>
        </div>
      </section>

      <section id="admin-section" class="mb-5 d-none">
        <h2>Administración</h2>
        <div id="app-users">
          <div class="alert alert-warning">Cargando usuarios...</div>
        </div>
      </section>
    {% endblock %}

    </main>

    {# jQuery, Bootstrap JS y tus scripts compilados #}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    ></script>

    {% block javascripts %}
      <script type="module" src="{{ asset('build/main.js') }}"></script>
    {% endblock %}
  </body>
</html>







